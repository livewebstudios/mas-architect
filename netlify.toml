[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Decap CMS SPA — keep deep links resolving to the admin shell
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
